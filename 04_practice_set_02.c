#include <stdio.h>

int main()
{
    int radius = 2;
    float pi = 3.14;
    printf("Area of circle is %f cmsq \n",pi*radius*radius);
    int height = 10;
    printf("Volume of cylinder is %f cmcube \n",pi*radius*radius*height);
    return 0;
}